from django.urls import path, re_path
# from .views import HomeView, ArticleView

# app_name = ''
urlpatterns = [
    # path('', HomeView.as_view(), name='home'),
    # path("articles/<int:pk>/something/", ArticleView.as_view(), name="article"),
    # re_path(r'^windy/$', windy, name='windy'),
]

